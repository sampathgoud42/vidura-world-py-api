[project]
name = "vidura-world"
version = "1.0.0"
description = "Multi-user REST backend for 38trades bots, tennis models, and the wellness app"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
