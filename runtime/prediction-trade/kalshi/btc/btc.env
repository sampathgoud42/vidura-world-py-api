# customer_folder intentionally unset — the API injects BTC_CUSTOMERS_DIR;
# standalone launches must export it explicitly
customer_folder=
# default_customer intentionally unset — the API injects BTC_CUSTOMER;
# standalone launches must export it explicitly
default_customer=